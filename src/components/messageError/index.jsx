// eslint-disable-next-line react/prop-types
const MessageErrorComponent = ({ children }) => {
  return <p style={{color: "red", marginTop: ".22rem", fontWeight: "500"}}>{children}</p>;
};

export default MessageErrorComponent
